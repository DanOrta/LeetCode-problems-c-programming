#include <stdio.h>
#include <string.h>
#include "main.h"

int main(void)
{
    RUN(twoSum);
    RUN(palindromeNumber);
    RUN(romanToInt);
    RUN(longestCommonPrefix);
    RUN(validParenthesis);
    RUN(mySqrt);

    return 0;
}
